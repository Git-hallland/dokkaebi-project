import { EditorialContentPage } from "@/components/EditorialContentPage"; import { getBoardCategory } from "@/lib/board-categories"; export default async function Page({params}:{params:Promise<{slug:string}>}){return <EditorialContentPage category={getBoardCategory("events")} type="events" slug={(await params).slug}/>}
